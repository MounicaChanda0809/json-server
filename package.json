{
  "name": "json-server",
  "version": "0.1.0",
  "description": "Fake REST API server using Json-server",
  "private": true,
  "author": "Mounica Chanda",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  }
}
